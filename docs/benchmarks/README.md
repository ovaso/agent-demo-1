# 历史基准档案

此目录保存旧提交的测量产物，不是当前性能基线，也不是可直接运行的基准套件。测试及基准源码在 `f6e832c` 删除；本轮没有重新运行这些测量。

| 产物 | 产品/源码提交 | 报告提交 | 当前可复现性 |
| --- | --- | --- | --- |
| `refactor-baseline.json` | 产品 `f57f08d`；基准源码 `f6df2a0` 的 `agent-core/examples/runtime_bench/` | `f6df2a0` | 当前 checkout 缺少程序。只能在旧源码、固定输入、相同工具链与配置下重新测量；不能保证相同数值。 |
| `refactor-comparison.json` | 产品基线 `f57f08d`；优化核心 `cd4cec9`；同一基准源码 `f6df2a0` | `3599363` | 同上；测量方法为交替运行进程，详见 JSON method。最终应用体积对应报告阶段，不能拿核心提交的产物替代。 |
| `cache-runtime-2026-09-10.md` | 产品基线 `78ec5ef`；缓存优化 `04640c1` | 初始报告 `bdc9b33`，整合说明 `98cf872` | 当前无完整可执行测量程序；报告未保留足以独立恢复全部固定场景的产物，不能宣称精确可复现。 |

JSON 的原始测量字段保持不变，附加 `_provenance` 用于说明历史范围。源码可通过 Git 历史查阅；本轮没有恢复、添加或执行任何基准程序。

缓存报告的“20 轮 3,015,387 字节”是累计模型 HTTP 请求 JSON 大小，**不是 SQLite 检查点写入量**。它不能证明全量检查点写入的实际耗时或 I/O 成本。检查点增量存储需要另外测量提交次数、序列化字节、数据库写入和恢复一致性。
