# 自适应运行时实现记录

按功能独立提交，设计目标见 [adaptive-agent-runtime.md](adaptive-agent-runtime.md)。本记录区分已交付能力与后续工作，不将设计草案视为已实现。

## 构建基线

- 工具链：`rustc 1.96.1 (31fca3adb 2026-06-26)`。
- 命令：`cargo build --release -p agent-app --locked`，默认 features，当前主机 target。
- 基线可执行文件：`target/release/agent-app`，5,790,864 字节。
- 未建立运行性能基准，不宣称耗时或内存收益。

## 交付顺序

1. 协议完整的上下文裁剪。
2. 可恢复 Loop、统一模型步数与逐工具检查点。
3. SQLite 运行存储与 CLI 恢复、状态和预算入口。
4. Plan、Blackboard 与受限只规划模式。
5. 模型路由、Graph 与途中模式切换。
6. 子 Agent 委托与共享预算。
7. 子 Agent 消息协作、等待和恢复。
8. 基于测量决定有界并发的实现与默认配置。

## 已交付

- 设计与领域术语入库。
- 上下文按完整工具批次裁剪，末尾批次在完成下一轮模型交互前保持完整；覆盖低历史上限和批次中途序列化恢复。此处消息数为软上限，单批数量和字节硬上限由后续运行时约束。
- 可恢复 Loop 核心：`Runtime::start/advance/resume/pause/cancel`、共享模型与工具预算、逐工具检查点、未知结果核实与显式重试、幂等会话收尾、内存后端的排他执行与版本比较。现有 Agent API 继续使用原入口，模型流式调用与追踪实现已抽取共用。
  - 8 项新故障与行为测试通过，workspace 的格式、Clippy 与全部测试通过；无默认 feature 核心测试通过（已有 ContextStore SQLite 辅助函数有 dead-code 警告）。
  - 同命令 release：5,791,248 字节，比初始基线增加 384 字节。新运行时尚未接入应用入口，该数字不代表后续完整功能的体积。
  - 此阶段检查点为有字节上限的完整快照，内存后端存在复制与序列化检查成本；尚未测量性能。SQLite 与后续多 Agent 的增量存储在各自阶段评估。
- SQLite 运行后端：复用连接，检查点与原会话表在同一事务内更新；同会话仅允许一个未结束任务，文件执行锁排除另一个执行者，版本比较拒绝过期保存。跨实例恢复、未知工具拦截、锁释放和事务冲突共 4 项测试通过。
  - 取消未执行工具会补齐协议结果，后续会话可以继续；取消不抹除历史检查点。SQLite 仍由原可选 feature 控制。
  - 格式、Clippy、workspace 测试和无默认 feature 测试均通过；同时修正了 SQLite 专用辅助函数在无默认 feature 下的已有警告。
  - release 为 5,791,312 字节，比初始基线增加 448 字节；SQLite 运行后端尚未接入 CLI。当前快照整行保存，会话内容未变化时不重复更新投影行；未测量持久化耗时。
- CLI 已切换到可恢复运行时：普通输入自动建立和执行任务，支持 `/start`、`/step`、`/pause`、`/resume`、`/status`、`/cancel`、`/budget`、`/resolve` 与 `/retry`；`--status` 可在没有 API 配置时查看任务。终端命令与会话装配从 main.rs 提取到 cli/。
  - 4 项命令解析与 CLI 跨实例恢复测试通过；格式、Clippy、workspace 与无默认 feature 测试均通过。
  - release 为 5,886,544 字节，相对初始基线增加 95,680 字节（约 1.65%），来自实际接入运行时和恢复入口；没有新增依赖。
  - 使用方式和当前限制见 [runtime-cli.md](runtime-cli.md)。
- 计划与 Blackboard 数据层：校验任务依赖 DAG、保留最多 16 个计划版本、禁止删除既有验收要求；共享记录保留修订、作者、来源版本与读取游标，通过运行检查点原子持久化。对冲突更新返回明确错误。
  - 新增计划依赖与版本、共享记录冲突与大小限制、SQLite 重开恢复测试；格式、Clippy、workspace 和无默认 feature 测试通过。
  - release 为 5,934,720 字节，比初始基线增加 143,856 字节；本阶段提供核心 API，模型规划入口在下一步接入。
- 模型规划与 Plan Mode：模型可提交计划、读写 Blackboard；只规划任务仅开放声明为只读的工具，运行时拦截未授权工具。计划交付后保持暂停，只有 `/execute` 转为执行，沿用原任务和预算。
  - CLI 增加 `/plan`、`/execute`、`/board`；新增普通文件读取、目录查看与固定参数 rg 搜索。普通任务按需规划，不强制额外规划调用。
  - 恢复兼容新增工具，但不会将它们自动授权给已有任务。模型不能伪造 Blackboard 的程序验证类型。
  - 新增核心权限边界、计划与预算延续、CLI 跨实例执行、只读工具边界测试；格式、Clippy、workspace 与无默认 feature 测试通过。
  - release 为 5,993,936 字节，相对上一阶段增加 59,216 字节，相对初始基线增加 203,072 字节；无新增依赖。当前 `/execute` 仍由 Loop 推进，Graph 调度为下一项功能。
- Graph 与模型路由：支持工具节点、独立上下文的 Agent Loop 节点、依赖调度、Loop/Graph 双向切换、节点失败拦截、最多 3 次尝试及历史结果保留。切换可发生在节点中途，工具批次结算和恢复预算保持完整。
  - CLI 增加 `/mode`、`/graph`、`/retry-node`；只规划阶段的模式选择留待 `/execute` 生效。新增固定 Rust 检查工具 `run_check`，复用现有有界进程输出捕获。
  - 工具结果增加业务成功标记；区分模型报告、工具成功、零退出码与操作者核实。节点内部文本不混入主回答，会话投影保持协调者上下文。
  - 复用完成节点时通过历史引用读取完整结果。固定 16 KiB 结果、8 次重规划的检查点从 230,699 字节降至 98,174 字节；未测量运行耗时收益。运行时记录的写入和共享记录更新会影响模型/只读结果的跨版本复用，验证命令重新运行。
  - 新增依赖顺序、双向切换、工具执行后切换、失败判定、重试上限、跨实例节点恢复、CLI 图选择与真实 fmt 检查等测试；格式、Clippy、workspace、无默认 feature 测试和 release 构建通过。
  - release 为 6,092,240 字节，相对上一阶段增加 98,304 字节，相对初始基线增加 301,376 字节（约 5.20%）；无新增依赖。独立委托、局部 Agent 预算和消息协作继续在此基础上实现。
- 独立 Agent 委托：Loop 可直接创建具名工作委托；各自拥有独立上下文、业务工具范围与局部累计模型步数。局部耗尽交回协调者，额度调整和恢复不重置根或局部用量；默认最多 8 次动态委托。
  - 采用协调者统一创建的平面团队和顺序调度，共享现有 ModelProvider，不增加异步运行时。只规划委托继承只读限制，节点不能递归创建或提升权限。Blackboard 写入校验条目作者，共享决策由协调者发布。
  - 增加模型委托、结果读取与预算管理工具，以及 `/agents`、`/agent-budget`、`/cancel-agent`；轨迹标注实际 actor，状态数据与固定运行规则分开传递。
  - 新增局部/根预算、权限与上下文隔离、只规划委托、取消计数、CLI 及 SQLite 跨实例恢复测试；格式、Clippy、workspace、无默认 feature 测试和 release 构建通过。
  - release 为 6,114,080 字节，比上一阶段增加 21,840 字节，比初始基线增加 323,216 字节（约 5.58%）；无新增依赖。定向消息和协作等待下一步接入。
- 子 Agent 消息协作：提供定向通知、请求、答复/拒绝、等待与收件箱游标；等待保存原工具调用并释放执行位置，答复后继续原批次。失败请求跳过尚未执行的后续工具，交回模型重新决定。
  - 同时检查任务依赖和消息等待环；按实际 actor、尝试和计划版本校验回复。相同答复幂等，旧计划请求不能推进新计划；请求、答复和消费位置随检查点原子保存。
  - 默认请求期限 5 分钟、最多 1 小时，按绝对时间在调度边界处理；每条正文 2048 字节，每个任务最多 256 条消息。通知和人工答复保持用户暂停状态。
  - CLI 增加 `/messages`、`/message`、`/reply`。新增单执行位置协作、批次顺序、拒绝/超时、环检测、幂等/身份、版本隔离、容量、SQLite 重开和 CLI 人工答复测试；格式、Clippy、workspace 与无默认 feature 测试全部通过。
  - release 为 6,173,808 字节，比上一阶段增加 59,728 字节，比初始基线增加 382,944 字节（约 6.61%）；无新增依赖。尚未对真实模型服务测量吞吐和并行收益。

## 当前边界

已经形成顺序执行的完整主路径：规划 → 选择 Loop/Graph → 委托与协作 → 验收 → 恢复或交付。当前采用平面团队、共享 ModelProvider、单个执行者；有界并行、不同 Provider 的角色池和更深的委托层级尚未实现。后续扩展应继续以独立功能提交，并用固定输入建立性能基线后再调整执行模型。
