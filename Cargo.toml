[workspace]
members = ["agent-app", "agent-app-tool", "agent-core", "agent-tool-macro", "agent-render-cmd", "agent-tool-debug"]
resolver = "3"

[profile.release]
opt-level = "z"
lto = "thin"
codegen-units = 1
panic = "abort"
strip = "symbols"
incremental = false
