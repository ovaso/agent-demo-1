# Agent 执行

本项目的执行设计围绕计划、Loop、Graph、执行路由、共享工作信息与多 Agent 协作展开。这里记录设计讨论中的领域术语，不代表相关能力已经实现。

## Language

**执行模式（Execution Mode）**：Agent 推进任务所采用的组织方式，包括 Loop 和 Graph；两种模式可以在同一任务中切换。
_Avoid_: 执行等级

**Loop**：由模型根据当前任务与执行结果逐步决定后续行动的执行模式。

**Graph**：以显式任务节点及其依赖或转移关系组织任务的执行模式；节点内可以使用 Loop。

**执行路由（Execution Routing）**：根据任务与当前进度选择或切换执行模式的决策。

**执行恢复（Resume）**：从已经保存的任务进度继续执行，延续已消耗的执行预算与未完成事项。
_Avoid_: 重新开始

**执行预算（Execution Budget）**：约束同一任务执行消耗的额度，包括最大执行步数；模式切换和执行恢复不产生新的额度。

**任务计划（Plan）**：对任务目标、约束、剩余工作与验收条件的显式描述；计划可以由 Loop 或 Graph 推进。
_Avoid_: Graph 的同义词

**计划模式（Plan Mode）**：以调查和形成任务计划为交付目标的工作模式；是否进入实际执行由用户指令或既有执行策略决定。

**重新规划（Replanning）**：根据新证据或任务变化形成后续计划版本，保留已发生的工作与尚未满足的要求。

**共享工作板（Blackboard）**：同一任务中供 Agent 协作使用的共享记录，包含有来源的观察、假设、决定、阻塞信息与产物引用。
_Avoid_: 全体 Agent 的聊天记录

**协调 Agent（Coordinator）**：负责理解整体任务、维护计划并分派工作的 Agent；可以同时承担规划和路由职责。

**子 Agent（Subagent）**：受委托完成有明确范围和交付要求的任务、拥有独立执行上下文的 Agent；其消耗属于所属任务的总预算。

**任务委托（Delegation）**：将界定范围、输入、产物和验收要求的工作交给另一个 Agent 执行的安排。

**协作请求（Collaboration Request）**：Agent 为完成已有委托而向另一 Agent 提出的信息、反馈或审查请求，具有明确的答复目标与结束条件。
_Avoid_: 新任务委托的同义词

**协作答复（Collaboration Reply）**：对一项协作请求给出的结果、拒绝或阻塞说明，可引用共享工作板中的证据与产物。

**协作等待（Collaboration Wait）**：Agent 因缺少特定答复、产物或前置任务结果而暂时不能继续推进的状态。

**结果接纳（Result Acceptance）**：确认某次执行的产物与证据适用于当前工作项、输入版本和验收条件的决定。
_Avoid_: 收到消息
