# Agent 执行

本项目区分应用运行环境、Shell 对话与任务执行；任务内围绕计划、Loop、Graph、共享工作信息与多 Agent 协作展开。这里记录设计讨论中的领域术语，不代表相关能力已经实现。

## Language

**运行事实（Runtime Facts）**：应用在本机观察到的环境变量、可用命令、系统版本及网络地址等事实，具有来源和观察时间；它们描述执行环境，不等同于模型可调用权限或长期记忆。

**Shell 对话模式（Shell Conversation Mode）**：应用启动后的默认交互方式，以精选会话上下文进行对话并识别任务意图；普通对话不自动创建任务运行。
_Avoid_: 操作系统命令执行器

**任务入口判断（Task Entry Decision）**：判断当前 Shell 输入应直接回复还是进入、继续或修订一个任务；用户显式选择计划或求解模式时直接采用其任务意图。

**执行模式（Execution Mode）**：Agent 推进任务所采用的组织方式，包括 Loop 和 Graph；两种模式可以在同一任务中切换。
_Avoid_: 执行等级

**Loop**：由模型根据当前任务与执行结果逐步决定后续行动的执行模式。

**Graph**：以显式任务节点及其依赖或转移关系组织任务的执行模式；节点内可以使用 Loop。

**执行路由（Execution Routing）**：根据任务与当前进度选择或切换执行模式的决策。

**执行恢复（Resume）**：从已经保存的任务进度继续执行，延续已消耗的执行预算与未完成事项。
_Avoid_: 重新开始

**执行预算（Execution Budget）**：约束同一任务执行消耗的额度，包括最大执行步数；模式切换和执行恢复不会重置已用额度或续期历史。

**已获准额度（Granted Budget）**：当前已经允许任务使用的累计额度。达到该额度时，需要评估是否满足预算续期条件。

**预算硬上限（Budget Ceiling）**：当前授权下自动执行不能突破的累计消耗上限。续期只能在这一上限以内发生。

**预算续期（Budget Extension）**：依据近期工作进展，在硬上限内有限增加已获准额度。续期有次数与单次增量约束，所有消耗仍属于原任务。

**任务计划（Plan）**：对任务目标、约束、剩余工作与验收条件的显式描述；计划可以由 Loop 或 Graph 推进。
_Avoid_: Graph 的同义词

**计划模式（Plan Mode）**：以调查和形成任务计划为交付目标的工作模式；是否进入实际执行由用户指令或既有执行策略决定。

**求解模式（Resolver Mode）**：直接推进当前获准任务并按需规划的工作模式；允许调查、执行及必要的计划分解，不要求先交付计划再开始执行。

**重新规划（Replanning）**：根据新证据或任务变化形成后续计划版本，保留已发生的工作与尚未满足的要求。

**共享工作板（Blackboard）**：同一任务中供 Agent 同步工作信息的共享记录，包含有来源的观察、假设、决定、阻塞信息与产物引用，并呈现任务、进度和依赖的有效状态。
_Avoid_: 全体 Agent 的聊天记录

**任务公共工作板（Task Blackboard）**：每个根任务都具有的公共协作入口，用于同步任务、进度、依赖和共享结论，并登记该任务中的临时协作工作板。

**临时协作工作板（Collaboration Blackboard）**：同一根任务内参与 Agent 围绕特定问题建立的局部共享记录，其成员、目的与生命周期由任务公共工作板登记；临时性不表示暂停或恢复后丢失协作依据。

**协调 Agent（Coordinator）**：负责理解整体任务、维护计划并分派工作的 Agent；可以同时承担规划和路由职责。

**主 Agent（Main Agent）**：承接根任务并负责整体交付的 Agent；默认承担协调职责，其执行同样受任务、验收条件与预算约束。

**子 Agent（Subagent）**：受委托完成有明确范围和交付要求的任务、拥有独立执行上下文的 Agent；其消耗属于所属任务的总预算。

**Agent 执行规格（Agent Execution Specification）**：描述一个 Agent 的当前任务、系统提示词、精选上下文、验收条件与资源授权的统一约定，适用于主 Agent 和子 Agent。
_Avoid_: 将子 Agent 委托说明视为另一套执行规格

**最小启动规格（Minimum Start Specification）**：Agent 执行规格达到可开始获准工作的最低完整程度；任务方向、适用规则、初始材料、验收依据和资源边界已明确，调查结论与验收证据可以在执行中形成。
_Avoid_: 另一种精简 Agent 类型

**精选上下文（Selected Context）**：依据当前任务及验收需要选出的、有来源和有效范围的上下文材料；未被选中不表示原始材料被删除。
_Avoid_: 完整父对话的同义词

**任务验收条件（Task Acceptance Criterion）**：判定当前任务交付是否满足要求的条件，包含所需证据及采用的判定方式；Agent 的完成说明本身不等于条件已满足。

**任务委托（Delegation）**：将界定范围、输入、产物和验收要求的工作交给另一个 Agent 执行的安排。

**协作请求（Collaboration Request）**：Agent 为完成已有委托而向另一 Agent 提出的信息、反馈或审查请求，具有明确的答复目标与结束条件。
_Avoid_: 新任务委托的同义词

**协作答复（Collaboration Reply）**：对一项协作请求给出的结果、拒绝或阻塞说明，可引用共享工作板中的证据与产物。

**协作等待（Collaboration Wait）**：Agent 因缺少特定答复、产物或前置任务结果而暂时不能继续推进的状态。

**结果接纳（Result Acceptance）**：确认某次执行的产物与证据适用于当前工作项、输入版本和验收条件的决定。
_Avoid_: 收到消息
